{% extends 'admin/common/base.html.twig' %}

{% block title %}
   Catégories
{% endblock %}

{% block body %}
   <div>
      {% for category in categories %}
         <div>
            <p>{{ category.name }}</p>
            <div>
               <img class="imgResp" src="{{ asset('uploads/pictures/'~category.picture) }}" alt="" title="">
            </div>
            <p>{{ category.wording }}</p>
            <div><a href="{{ path('admin_delete_category', {'id':category.id}) }}">Supprimer</a></div>
         </div>
      {% endfor %}
   </div>
{% endblock %}