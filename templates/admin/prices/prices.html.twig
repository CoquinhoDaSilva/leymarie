{% extends 'admin/common/base.html.twig' %}

{% block title %}
    Prix
{% endblock %}

{% block body %}

    <section class="displayFlexCenter">
        <div class="menuBdd container displayFlex">
            {% for price in prices %}
                <div class="prices xLarge-5 large-5 medium-6 small-12">
                    <div>
                        <a><h2 class="menuBddTitle textCenter">{{ price.value }} €</h2></a>
                    </div>
                    <div class="menuCrud displayFlex">
                        <div>
                            <p class="update"><a href="{{ path('admin_update_price', {'id':price.id}) }}">Modifier</a></p>
                        </div>
                        <div>
                            <p class="delete"><a href="{{ path('admin_delete_price', {'id':price.id}) }}">Supprimer</a></p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}