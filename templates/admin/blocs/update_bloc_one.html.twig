{% extends 'admin/common/base.html.twig' %}


{% block title %}
    Mise à jour du premier bloc
{% endblock %}


{% block body %}
    <div class="displayFlexCenter">
        <div class="container">
            <div>
                <div>
                    <h2 class="articleTitle">{{ blocOneView.title }}</h2>
                </div>
                {% if blocOneView.picture %}
                <div class="displayFlexCenter">
                    <div class="xLarge-6">
                        <img class="imgResp" src="{{ asset('uploads/pictures/' ~ blocOne.picture) }}" alt="">
                    </div>
                </div>
                {% endif %}
                <div class="articleBorderContent">
                    <p>{{ blocOneView.content | raw}}</p>
                </div>
            </div>
            {#
            <div class="menuCrud displayFlex">
                <div>
                    <p class="btnUpdate"><a href="{{ path('admin_update_article', {'id':article.id}) }}">Modifier</a></p>
                </div>
                <div>
                    <p class="btnDelete"><a href="{{ path('admin_delete_article', {'id':article.id}) }}">Supprimer</a></p>
                </div>
            </div>
            #}
        </div>
    </div>
{% endblock %}